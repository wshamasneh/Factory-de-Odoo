---
phase: 03-module-discussion
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - tests/discuss-module.test.cjs
  - odoo-gsd/references/module-questions.json
autonomous: true
requirements:
  - DISC-02
  - DISC-03

must_haves:
  truths:
    - "module-questions.json contains all 10 module types (9 named + generic) with 8-12 questions each"
    - "Each question object has id, question, and context fields"
    - "Type detection from module name returns correct type for all 9 known prefixes and null for unknown"
  artifacts:
    - path: "odoo-gsd/references/module-questions.json"
      provides: "Question templates for all module types"
      contains: "fee"
    - path: "tests/discuss-module.test.cjs"
      provides: "Test coverage for type detection, template loading, template schema"
      min_lines: 80
  key_links:
    - from: "tests/discuss-module.test.cjs"
      to: "odoo-gsd/references/module-questions.json"
      via: "fs.readFileSync and JSON.parse"
      pattern: "module-questions\\.json"
---

<objective>
Create the question template data and Wave 0 test scaffold for Phase 3.

Purpose: Establish the foundational data (module-questions.json with 10 type templates) and automated tests that validate all Phase 3 artifacts. This is Wave 0 -- tests must exist before implementation tasks can verify themselves.

Output: `odoo-gsd/references/module-questions.json` (10 templates), `tests/discuss-module.test.cjs` (test file covering DISC-01 through DISC-05, AGNT-01)
</objective>

<execution_context>
@/home/inshal-rauf/.claude/get-shit-done/workflows/execute-plan.md
@/home/inshal-rauf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-module-discussion/03-CONTEXT.md
@.planning/phases/03-module-discussion/03-RESEARCH.md
@.planning/phases/03-module-discussion/03-VALIDATION.md

@tests/new-erp.test.cjs
@tests/agent-frontmatter.test.cjs
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Create test scaffold for Phase 3 (Wave 0)</name>
  <files>tests/discuss-module.test.cjs</files>
  <behavior>
    - Test: module-questions.json exists and is valid JSON
    - Test: all 10 types present (core, student, fee, exam, faculty, hr, timetable, notification, portal, generic)
    - Test: each type has 8-12 questions in its questions array
    - Test: each question object has required fields (id, question, context)
    - Test: each question id is unique within its type
    - Test: type detection function returns 'fee' for 'uni_fee', 'student' for 'uni_student', etc. for all 9 known prefixes
    - Test: type detection returns null for unknown module names like 'custom_thing'
    - Test: discuss-module command file exists with correct frontmatter (name: odoo-gsd:discuss-module, allowed-tools includes Task and AskUserQuestion)
    - Test: discuss-module workflow file exists and references the questioner agent
    - Test: odoo-gsd-module-questioner agent file exists with AskUserQuestion in tools
    - Test: odoo-gsd-module-researcher agent file contains enhanced Odoo domain sections (field type, security pattern, view inheritance)
  </behavior>
  <action>
Create `tests/discuss-module.test.cjs` using `node:test` and `node:assert/strict` (same pattern as `tests/new-erp.test.cjs`).

Structure:
1. Constants: `AGENTS_DIR`, `COMMANDS_DIR`, `WORKFLOWS_DIR`, `REFERENCES_DIR` paths relative to `__dirname/..`
2. `MODULE_TYPES` constant: array of 10 types including 'generic'
3. `KNOWN_PREFIXES` map: `{ 'uni_core': 'core', 'uni_student': 'student', 'uni_fee': 'fee', 'uni_exam': 'exam', 'uni_faculty': 'faculty', 'uni_hr': 'hr', 'uni_timetable': 'timetable', 'uni_notification': 'notification', 'uni_portal': 'portal' }`

Test groups:
- `describe('TMPL: module-questions.json schema')` -- validates JSON file structure, 10 types, 8-12 questions per type, required fields
- `describe('TYPE: module type detection')` -- inline detection function (same logic the workflow uses), test all 9 prefixes return correct type, unknown returns null
- `describe('CMD: discuss-module command')` -- command file exists, has correct name/allowed-tools in frontmatter
- `describe('WF: discuss-module workflow')` -- workflow file exists, references questioner agent
- `describe('AGNT: questioner agent')` -- agent file exists, has AskUserQuestion in tools, has skills field
- `describe('AGNT: enhanced researcher')` -- researcher agent file contains 'field type', 'security pattern', 'view inheritance' sections

The type detection function to test (extract as a helper at the top of the test file):
```javascript
function detectModuleType(moduleName) {
  const TYPES = {
    'uni_core': 'core', 'uni_student': 'student', 'uni_fee': 'fee',
    'uni_exam': 'exam', 'uni_faculty': 'faculty', 'uni_hr': 'hr',
    'uni_timetable': 'timetable', 'uni_notification': 'notification',
    'uni_portal': 'portal'
  };
  if (TYPES[moduleName]) return TYPES[moduleName];
  for (const [prefix, type] of Object.entries(TYPES)) {
    if (moduleName.startsWith(prefix + '_')) return type;
  }
  return null;
}
```

Run tests after creation -- they should FAIL (RED phase) since module-questions.json, command, workflow, and agent files do not yet exist.
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_GSD && node --test tests/discuss-module.test.cjs 2>&1 | tail -5</automated>
  </verify>
  <done>Test file exists with all test groups. Tests fail because artifacts do not yet exist (RED phase).</done>
</task>

<task type="auto">
  <name>Task 2: Create module-questions.json with all 10 type templates</name>
  <files>odoo-gsd/references/module-questions.json</files>
  <action>
Create `odoo-gsd/references/module-questions.json` with 10 entries. Each entry has `questions` array (8-12 items) and `context_hints` array.

Question object schema: `{ "id": string, "question": string, "options": string[]|null, "default": string|null, "context": string }`

**Template content by type (domain-specific with Odoo knowledge baked in per CONTEXT.md locked decision):**

**core** (10 questions): institution hierarchy (single/multi-campus), academic year/term structure, department organization, program/course structure, code generation patterns (auto vs manual), company per campus (if multi_company), base security groups, menu hierarchy, sequence numbering, localization hooks.

**student** (10 questions): enrollment states (applied/admitted/enrolled/graduated/withdrawn + custom), academic holds (financial/disciplinary/academic), document management (upload types, verification), student ID generation, guardian/parent tracking, photograph/biometric, transfer handling, alumni tracking, batch/section grouping, portal self-service scope.

**fee** (10 questions): monetary vs float field type, penalty method (fixed/percentage/tiered), payment channels (manual/bank/gateway), installment plans, fee structure per program, scholarship/concession handling, refund policy, invoice generation (account.move integration), receipt printing, fee component types (tuition/lab/activity/hostel).

**exam** (10 questions): grading scales (GPA/percentage/grade letters), makeup/re-exam policy, grade computation method (weighted/simple average), result publishing workflow (draft/verified/published), exam types (midterm/final/quiz/assignment), seating plan generation, date sheet management, hall ticket generation, external examiner support, mark entry workflow (single/double-blind).

**faculty** (10 questions): workload tracking (credit hours/contact hours), leave integration (hr.leave), performance evaluation method, designation hierarchy, specialization tracking, research profile, teaching assignment, course allocation method, faculty portal scope, contract type (permanent/visiting/adjunct).

**hr** (10 questions): payroll integration (hr.payroll), contract types, attendance method (biometric/manual/both), overtime policy, department-wise payslip, loan/advance management, employee document management, probation tracking, appraisal cycles, leave encashment.

**timetable** (10 questions): conflict resolution strategy (hard constraint/soft preference), room allocation (manual/auto), recurring patterns (weekly/bi-weekly), time slot granularity (30min/1hr), faculty availability, room types (lab/lecture/seminar), substitution handling, timetable publication, clash detection scope (room+faculty+student), template support.

**notification** (10 questions): channel selection (email/sms/whatsapp/push), template engine (qweb/jinja/plain), event triggers (enrollment/fee/exam/attendance), delivery tracking, bulk sending, scheduling (immediate/queued/scheduled), recipient resolution (student/guardian/faculty), opt-out handling, retry policy, notification log retention.

**portal** (10 questions): role-based access (student/faculty/parent/admin), self-service scope (view grades/pay fees/download documents), document upload types, mobile responsiveness, dashboard widgets, SSO integration, session timeout, password policy, announcement board, feedback/grievance module.

**generic** (8 questions): primary models and their relationships, key workflows and state transitions, security groups and access levels, integration points with other modules, reporting needs, portal exposure, automation/cron requirements, data import/migration needs.

Each question must have an `id` following `{type}_{short_name}` pattern (e.g., `fee_field_type`, `exam_grading_scale`).
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_GSD && node --test tests/discuss-module.test.cjs --test-name-pattern "TMPL" 2>&1 | tail -5</automated>
  </verify>
  <done>module-questions.json exists with 10 types, each having 8-12 questions with id/question/context fields. TMPL tests pass (GREEN for template tests).</done>
</task>

</tasks>

<verification>
- `node --test tests/discuss-module.test.cjs --test-name-pattern "TMPL"` passes (template schema valid)
- `node --test tests/discuss-module.test.cjs --test-name-pattern "TYPE"` passes (type detection works)
- `module-questions.json` is valid JSON parseable by `JSON.parse(fs.readFileSync(...))`
</verification>

<success_criteria>
- module-questions.json has exactly 10 top-level keys
- Each type has 8-12 questions with id, question, context fields
- Type detection returns correct type for all 9 uni_* prefixes
- Test file covers all 6 requirement IDs (DISC-01 through DISC-05, AGNT-01)
- Template and type detection tests pass; remaining tests fail (artifacts not yet created)
</success_criteria>

<output>
After completion, create `.planning/phases/03-module-discussion/03-01-SUMMARY.md`
</output>
