---
phase: 01-fork-foundation-and-state-infrastructure
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - get-shit-done/ -> odoo-gsd/ (directory rename)
  - odoo-gsd/bin/odoo-gsd-tools.cjs (renamed from gsd-tools.cjs)
  - odoo-gsd/bin/lib/core.cjs (MODEL_PROFILES keys)
  - odoo-gsd/bin/lib/config.cjs (.gsd/ path references)
  - odoo-gsd/bin/install.js (full rewrite)
  - commands/odoo-gsd/ (renamed from commands/gsd/)
  - agents/*.md (12 files renamed gsd-* to odoo-gsd-*)
  - hooks/*.md (3 files renamed)
  - workflows/*.md (12+ files, internal references)
  - references/*.md (internal references)
  - templates/*.md (internal references)
  - package.json
  - CLAUDE.md
  - tests/helpers.cjs
autonomous: true
requirements:
  - FORK-01
  - FORK-02
  - FORK-03
  - FORK-04
  - FORK-05
  - FORK-06
  - FORK-07
  - FORK-08
  - FORK-09
  - FORK-10
  - TEST-05

must_haves:
  truths:
    - "Running `node odoo-gsd/bin/odoo-gsd-tools.cjs` executes without error"
    - "grep -r 'get-shit-done\\|/gsd:\\|gsd-tools' finds zero matches in codebase (excluding node_modules, .git)"
    - "All 16 existing tests pass with `npm test` after rename"
    - "install.js is Claude-Code-only and checks for odoo-gen + Python 3.8+"
  artifacts:
    - path: "odoo-gsd/bin/odoo-gsd-tools.cjs"
      provides: "Renamed CLI entry point"
    - path: "odoo-gsd/bin/install.js"
      provides: "Claude-Code-only installer with odoo-gen and Python checks"
    - path: "odoo-gsd/bin/lib/core.cjs"
      provides: "MODEL_PROFILES with odoo-gsd-* keys"
    - path: "commands/odoo-gsd/"
      provides: "32 command files with /odoo-gsd: prefix"
    - path: "CLAUDE.md"
      provides: "Odoo ERP orchestrator context document"
    - path: "package.json"
      provides: "Renamed package with odoo-gsd identity"
    - path: "tests/helpers.cjs"
      provides: "Updated TOOLS_PATH pointing to odoo-gsd"
  key_links:
    - from: "tests/helpers.cjs"
      to: "odoo-gsd/bin/odoo-gsd-tools.cjs"
      via: "TOOLS_PATH constant"
      pattern: "odoo-gsd.*odoo-gsd-tools"
    - from: "odoo-gsd/bin/odoo-gsd-tools.cjs"
      to: "odoo-gsd/bin/lib/*.cjs"
      via: "require() calls"
      pattern: "require.*lib/"
    - from: "workflows/*.md"
      to: "odoo-gsd/bin/odoo-gsd-tools.cjs"
      via: "Tool invocation paths"
      pattern: "odoo-gsd-tools"
---

<objective>
Rename the entire GSD fork to odoo-gsd: directory structure, CLI entry point, command prefix, agent names, workflow references, documentation, and package identity. Rewrite install.js for Claude Code only with odoo-gen and Python checks.

Purpose: Establish the odoo-gsd identity as the foundation for all subsequent Phase 1 work. Every other plan depends on correct paths and command names.
Output: A fully renamed codebase where all 16 existing tests pass and zero old references remain.
</objective>

<execution_context>
@/home/inshal-rauf/.claude/get-shit-done/workflows/execute-plan.md
@/home/inshal-rauf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-fork-foundation-and-state-infrastructure/01-RESEARCH.md

@tests/helpers.cjs
@get-shit-done/bin/lib/core.cjs
@get-shit-done/bin/gsd-tools.cjs
@package.json
</context>

<tasks>

<task type="auto">
  <name>Task 1: Rename directory structure and all internal references</name>
  <files>
    get-shit-done/ -> odoo-gsd/
    odoo-gsd/bin/gsd-tools.cjs -> odoo-gsd/bin/odoo-gsd-tools.cjs
    commands/gsd/ -> commands/odoo-gsd/
    agents/gsd-*.md -> agents/odoo-gsd-*.md
    hooks/gsd-*.md -> hooks/odoo-gsd-*.md
    odoo-gsd/bin/lib/core.cjs
    odoo-gsd/bin/lib/config.cjs
    odoo-gsd/bin/lib/commands.cjs
    odoo-gsd/bin/lib/state.cjs
    odoo-gsd/bin/lib/phase.cjs
    odoo-gsd/bin/lib/init.cjs
    odoo-gsd/workflows/*.md
    odoo-gsd/references/*.md
    odoo-gsd/templates/*.md
    tests/helpers.cjs
    package.json
  </files>
  <action>
    Execute the rename in strict layers, verifying each layer with grep before proceeding:

    **Layer 1 - Directory rename:**
    - `git mv get-shit-done odoo-gsd` (single commit preserving history)
    - `git mv commands/gsd commands/odoo-gsd`

    **Layer 2 - CLI entry point rename:**
    - `git mv odoo-gsd/bin/gsd-tools.cjs odoo-gsd/bin/odoo-gsd-tools.cjs`

    **Layer 3 - Agent and hook file renames:**
    - Rename all 12 agent files: `gsd-*.md` -> `odoo-gsd-*.md` in agents/
    - Rename all 3 hook files: `gsd-*.md` -> `odoo-gsd-*.md` in hooks/

    **Layer 4 - Internal reference updates (sed across all .cjs, .md, .js files):**
    - In core.cjs: Update all 12 MODEL_PROFILES keys from `gsd-*` to `odoo-gsd-*`
    - In config.cjs: Update `.gsd/` references to `.odoo-gsd/` (brave_api_key path, defaults.json path)
    - In odoo-gsd-tools.cjs: Update `gsd-tools` references, require paths
    - In all lib/*.cjs files: Update `get-shit-done` path references to `odoo-gsd`
    - In all workflow .md files: Update `gsd-tools.cjs` to `odoo-gsd-tools.cjs`, `/gsd:` to `/odoo-gsd:`
    - In all command .md files (commands/odoo-gsd/): Update frontmatter prefix from `/gsd:` to `/odoo-gsd:`
    - In all agent .md files: Update path references and command references
    - In all template .md files: Update tool invocation paths

    **Layer 5 - Test infrastructure:**
    - Update `tests/helpers.cjs`: Change TOOLS_PATH to `path.join(__dirname, '..', 'odoo-gsd', 'bin', 'odoo-gsd-tools.cjs')`
    - Update temp directory prefix from `gsd-test-` to `odoo-gsd-test-`

    **Layer 6 - Package identity:**
    - Update `package.json`: name to `odoo-gsd`, description to "Odoo ERP module orchestrator for Claude Code", remove multi-runtime keywords, update repository URL

    **Verification after each layer:** Run `grep -r "get-shit-done\|gsd-tools\|/gsd:" --include="*.cjs" --include="*.md" --include="*.js" --include="*.json" . | grep -v node_modules | grep -v .git` and fix any remaining references.

    **CRITICAL anti-pattern to avoid:** Do NOT create double-renames (e.g., `odoo-odoo-gsd`). When replacing `gsd` with `odoo-gsd`, use exact string matching not regex wildcards. Replace `get-shit-done` with `odoo-gsd`, `/gsd:` with `/odoo-gsd:`, `gsd-tools` with `odoo-gsd-tools`, `gsd-planner` with `odoo-gsd-planner`, etc. -- always full token matches.
  </action>
  <verify>
    <automated>grep -r "get-shit-done\|gsd-tools\.cjs\|/gsd:\|\"gsd-" --include="*.cjs" --include="*.md" --include="*.js" --include="*.json" . | grep -v node_modules | grep -v ".git/" | wc -l</automated>
    Result must be 0. Then: `npm test` -- all 16 existing tests must pass.
  </verify>
  <done>Zero remaining old references. All 16 existing tests pass. Directory structure matches research architecture diagram (odoo-gsd/bin/, commands/odoo-gsd/, agents/odoo-gsd-*.md).</done>
</task>

<task type="auto">
  <name>Task 2: Rewrite install.js and CLAUDE.md</name>
  <files>
    odoo-gsd/bin/install.js
    CLAUDE.md
  </files>
  <action>
    **install.js rewrite (target ~300 LOC, down from 2464):**

    Strip ALL non-Claude-Code runtime support. The new install.js should:
    1. Detect Claude Code installation (`~/.claude/` directory exists)
    2. Create symlink or copy `odoo-gsd/` to `~/.claude/odoo-gsd/`
    3. Register slash commands from `commands/odoo-gsd/` directory
    4. Check for odoo-gen at `~/.claude/odoo-gen/` -- warn if missing with install instructions (FORK-06)
    5. Check for Python 3.8+ via `python3 --version` -- warn if missing (FORK-07)
    6. Validate that required directories exist
    7. Print success summary with available commands

    Do NOT preserve: OpenCode detection/config, Gemini CLI detection/config, Codex CLI detection/config, multi-runtime settings management.

    Hard-delete all conditional runtime branches. Start clean from the Claude Code detection path only.

    **CLAUDE.md rewrite:**

    Create a new CLAUDE.md as the Odoo ERP orchestrator context document containing:
    1. Project identity: odoo-gsd -- Odoo ERP module orchestrator built on Claude Code
    2. Command reference: all `/odoo-gsd:*` commands with brief descriptions
    3. Architecture: state files (.planning/model_registry.json, module_status.json, config.json), module lifecycle (planned -> spec_approved -> generated -> checked -> shipped)
    4. Rules: sequential generation only, all state changes through CLI subcommands, atomic writes for JSON state, immutable data patterns
    5. File locations: where each state file lives, what each lib module does
    6. Development: how to run tests, coverage requirements (80%+), CJS module format, zero npm runtime dependencies
  </action>
  <verify>
    <automated>node -c odoo-gsd/bin/install.js && wc -l odoo-gsd/bin/install.js | awk '{if ($1 > 500) exit 1; else print "OK: " $1 " lines"}'</automated>
    install.js must parse without syntax errors and be under 500 lines. CLAUDE.md must exist.
  </verify>
  <done>install.js is Claude-Code-only (~300 LOC), checks for odoo-gen and Python 3.8+. CLAUDE.md documents the odoo-gsd architecture, commands, and rules. No references to OpenCode, Gemini, or Codex remain in install.js.</done>
</task>

</tasks>

<verification>
1. `grep -r "get-shit-done\|gsd-tools\|/gsd:\|\"gsd-" --include="*.cjs" --include="*.md" --include="*.js" --include="*.json" . | grep -v node_modules | grep -v .git` returns zero matches (FORK-10)
2. `npm test` passes all 16 existing tests (TEST-05)
3. `node odoo-gsd/bin/odoo-gsd-tools.cjs --help` or basic invocation works
4. `node -c odoo-gsd/bin/install.js` parses without error
5. `test -f CLAUDE.md` confirms CLAUDE.md exists
</verification>

<success_criteria>
- Zero references to old names in codebase (get-shit-done, /gsd:, gsd-tools, gsd-planner etc.)
- All 16 existing tests pass with `npm test`
- install.js is Claude-Code-only, under 500 LOC, checks odoo-gen + Python 3.8+
- CLAUDE.md documents odoo-gsd architecture and commands
- package.json has odoo-gsd name and description
</success_criteria>

<output>
After completion, create `.planning/phases/01-fork-foundation-and-state-infrastructure/01-01-SUMMARY.md`
</output>
