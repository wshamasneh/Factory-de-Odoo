---
phase: 04-spec-generation-and-coherence
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - agents/odoo-gsd-spec-generator.md
  - agents/odoo-gsd-spec-reviewer.md
  - tests/agent-frontmatter.test.cjs
autonomous: true
requirements: [SPEC-02, SPEC-03, AGNT-02, AGNT-04]

must_haves:
  truths:
    - "Spec generator agent produces spec.json with all 12 sections plus _available_models from provided context"
    - "Spec reviewer agent reads coherence report and spec.json to produce human-readable summary"
    - "Both agents have correct frontmatter for Task() spawning via subagent_type"
  artifacts:
    - path: "agents/odoo-gsd-spec-generator.md"
      provides: "Spec generation agent that outputs spec.json"
      contains: "spec-generator"
    - path: "agents/odoo-gsd-spec-reviewer.md"
      provides: "Coherence presentation and spec summary agent"
      contains: "spec-reviewer"
    - path: "tests/agent-frontmatter.test.cjs"
      provides: "Frontmatter validation for new agents"
      contains: "spec-generator"
  key_links:
    - from: "agents/odoo-gsd-spec-generator.md"
      to: "spec.json schema"
      via: "agent instructions"
      pattern: "models.*fields.*business_rules.*computation_chains"
    - from: "agents/odoo-gsd-spec-reviewer.md"
      to: "coherence report format"
      via: "agent instructions"
      pattern: "many2one_targets.*duplicate_models.*computed_depends.*security_groups"
---

<objective>
Create the two new agent files: spec generator (produces spec.json) and spec reviewer (presents coherence results to humans).

Purpose: These agents are the LLM-powered components of the plan-module pipeline. The spec generator translates human decisions (CONTEXT.md) and research (RESEARCH.md) into a structured spec.json contract. The reviewer formats the deterministic coherence report into an actionable human summary.

Output: Two agent markdown files + extended agent frontmatter tests
</objective>

<execution_context>
@/home/inshal-rauf/.claude/get-shit-done/workflows/execute-plan.md
@/home/inshal-rauf/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-spec-generation-and-coherence/04-CONTEXT.md
@.planning/phases/04-spec-generation-and-coherence/04-RESEARCH.md

<interfaces>
<!-- Existing agent file format (from odoo-gsd-module-questioner.md pattern) -->
Agent files use markdown with YAML frontmatter containing:
- name: agent identifier for subagent_type matching
- description: what the agent does
- tools: allowed tool list

The agent body contains instructions, context expectations, and output format requirements.

Spec.json full schema (locked):
```json
{
  "module": "uni_fee",
  "version": "17.0",
  "models": [{ "name": "...", "description": "...", "inherit": null, "fields": [...] }],
  "business_rules": [...],
  "computation_chains": [{ "name": "...", "steps": [{"model": "...", "field": "...", "trigger": "..."}] }],
  "workflow": [...],
  "view_hints": [{ "model": "...", "view_type": "...", "key_fields": [...], "notes": "..." }],
  "reports": [...],
  "notifications": [...],
  "cron_jobs": [...],
  "security": { "groups": [...], "access_rules": [...], "record_rules": [...] },
  "portal": [...],
  "api_endpoints": [...],
  "_available_models": { "direct": {...}, "transitive": {...}, "rest": [...] }
}
```

Coherence report format (locked):
```json
{
  "status": "pass|fail",
  "checks": [
    {"check": "many2one_targets", "status": "pass|fail", "violations": [...]},
    {"check": "duplicate_models", "status": "pass|fail", "violations": [...]},
    {"check": "computed_depends", "status": "pass|fail", "violations": [...]},
    {"check": "security_groups", "status": "pass|fail", "violations": [...]}
  ]
}
```

From tests/agent-frontmatter.test.cjs:
Tests validate that agent files have correct frontmatter fields (name, description, tools).
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create spec generator and reviewer agent files</name>
  <files>agents/odoo-gsd-spec-generator.md, agents/odoo-gsd-spec-reviewer.md</files>
  <action>
    1. Create agents/odoo-gsd-spec-generator.md:
       - Frontmatter: name "odoo-gsd-spec-generator", description about producing spec.json from module context
       - Tools: Read, Write, Bash (for reading files and writing spec.json)
       - Instructions section explaining:
         a. Agent receives: module name, CONTEXT.md content, RESEARCH.md content, decomposition entry, config.json odoo block, tiered registry JSON
         b. Must produce a complete spec.json with ALL 12 sections: models, fields (within models), business_rules, computation_chains, workflow, view_hints, reports, notifications, cron_jobs, security, portal, api_endpoints
         c. Plus _available_models section (passed in from tiered registry -- agent writes it as-is into spec.json)
         d. Models use array format (not object): [{name, description, inherit, fields: [{name, type, ...}]}]
         e. Computation chains: [{name, steps: [{model, field, trigger}]}] -- no field type info
         f. View hints: [{model, view_type, key_fields, notes}] -- no XML
         g. Security: {groups, access_rules: [{model, group, perm_read, perm_write, perm_create, perm_unlink}], record_rules: [{name, model, domain (natural language), description}]}
         h. Output path: Write spec.json to `.planning/modules/{module}/spec.json`
         i. Single-shot: produce ALL sections in one pass, do not split across multiple writes
       - Odoo-specific knowledge section: field type reference (Char, Integer, Float, Monetary, Boolean, Date, Datetime, Text, Html, Binary, Selection, Many2one, One2many, Many2many), common patterns (compute/store/depends, related fields, onchange)

    2. Create agents/odoo-gsd-spec-reviewer.md:
       - Frontmatter: name "odoo-gsd-spec-reviewer", description about presenting coherence results
       - Tools: Read, Bash (for reading files), no Write (read-only agent)
       - Instructions section explaining:
         a. Agent receives: coherence report JSON, spec.json path, module name
         b. Present coherence report first: pass/fail per check, list all violations with Odoo-specific context
         c. If coherence passes: show spec summary (model count, total field count, fields per model, business rules count, computation chain count, security groups count, view hints count)
         d. NOT the full JSON -- just summary stats and key highlights
         e. For violations, add Odoo context (e.g., "Many2one to uni.enrollment -- this model should be generated by uni_student module in Tier 2")
         f. End with recommendation: approve, revise, or investigate specific violations
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_GSD && SECTIONS="models business_rules computation_chains workflow view_hints reports notifications cron_jobs security portal api_endpoints _available_models" && PASS=true && for s in $SECTIONS; do grep -q "$s" agents/odoo-gsd-spec-generator.md || { echo "MISSING section: $s in spec-generator"; PASS=false; }; done && grep -q "spec-generator" agents/odoo-gsd-spec-generator.md && grep -q "spec-reviewer" agents/odoo-gsd-spec-reviewer.md && grep -q "many2one_targets\|coherence" agents/odoo-gsd-spec-reviewer.md && $PASS && echo "PASS"</automated>
  </verify>
  <done>Both agent files exist with correct frontmatter. Spec generator mentions all 12 spec.json sections plus _available_models. Spec reviewer references coherence report checks.</done>
</task>

<task type="auto">
  <name>Task 2: Extend agent frontmatter tests for new agents</name>
  <files>tests/agent-frontmatter.test.cjs</files>
  <action>
    Read existing tests/agent-frontmatter.test.cjs to understand the test pattern.

    Add test cases for:
    1. odoo-gsd-spec-generator.md: verify file exists, has frontmatter with name containing "spec-generator", has description, has tools array including Write
    2. odoo-gsd-spec-reviewer.md: verify file exists, has frontmatter with name containing "spec-reviewer", has description, has tools array NOT including Write (read-only)
    3. Verify spec generator instructions mention all 12 spec.json sections (models, business_rules, computation_chains, workflow, view_hints, reports, notifications, cron_jobs, security, portal, api_endpoints, _available_models)

    Follow the exact pattern used by existing agent tests in the file.
  </action>
  <verify>
    <automated>cd /home/inshal-rauf/Odoo_GSD && node --test tests/agent-frontmatter.test.cjs && npm test</automated>
  </verify>
  <done>Agent frontmatter tests pass for both new agents. Full test suite passes with zero regressions.</done>
</task>

</tasks>

<verification>
1. `node --test tests/agent-frontmatter.test.cjs` -- all agent tests pass including new ones
2. `npm test` -- full suite passes
3. Both agent files have valid markdown structure with frontmatter
4. Spec generator agent body mentions all 12 spec.json sections
</verification>

<success_criteria>
- odoo-gsd-spec-generator.md covers all 12 spec.json sections with locked formats
- odoo-gsd-spec-reviewer.md formats coherence reports with Odoo context
- Agent frontmatter tests extended and passing
- Zero regressions
</success_criteria>

<output>
After completion, create `.planning/phases/04-spec-generation-and-coherence/04-02-SUMMARY.md`
</output>
